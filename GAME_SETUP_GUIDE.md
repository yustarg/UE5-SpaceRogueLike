# 🎮 太空 Roguelike 游戏 - 完整配置指南

## 📖 文档说明

本文档详细说明从零开始配置游戏的**每一个步骤**，包括创建蓝图、配置参数、设置关卡等。

**预计完成时间**: 2-4 小时  
**难度**: ⭐⭐⭐ (中等)  
**前置要求**: C++ 代码已编译成功

---

## 📋 配置清单概览

### 需要创建的资产
- [ ] 5 个蓝图类（GameMode、玩家、敌人、子弹、能力）
- [ ] 3 个输入资产（Input Actions 和 Mapping Context）
- [ ] 2 个 UI Widget（HUD 和 GameOver）
- [ ] 1 个游戏关卡
- [ ] 若干美术资产（可选，可用基础形状代替）

---

## 🚀 第一阶段：创建基础蓝图类

### 1.1 创建游戏模式蓝图

**步骤**：

1. **打开内容浏览器**
   - 在 Unreal Editor 底部找到 "Content Browser" 面板
   - 如果看不到，点击菜单 `Window` → `Content Browser`

2. **创建 Blueprints 文件夹**
   - 在 `Content` 根目录下，右键 → `New Folder`
   - 命名为 `Blueprints`
   - 双击进入该文件夹

3. **创建 Core 子文件夹**
   - 在 `Blueprints` 文件夹内，再创建一个文件夹
   - 命名为 `Core`
   - 用于存放核心游戏逻辑蓝图

4. **创建 GameMode 蓝图**
   - 在 `Core` 文件夹内，右键空白处
   - 选择 `Blueprint Class`
   - 在弹出的窗口中，点击 "All Classes" 下拉菜单
   - 搜索并选择 `SpaceRoguelikeGameMode` (这是你的 C++ 类)
   - 如果找不到，检查代码是否编译成功
   - 点击 `Select`
   - 命名为 `BP_SpaceRoguelikeGameMode`

5. **配置 GameMode**
   - 双击打开 `BP_SpaceRoguelikeGameMode`
   - 暂时不需要修改任何内容（稍后会设置 Pawn 类）
   - 点击 `Compile` → `Save` → 关闭

---

### 1.2 创建玩家飞船蓝图

**步骤**：

1. **创建 Player 文件夹**
   - 回到 `Blueprints` 文件夹
   - 创建新文件夹：`Player`

2. **创建飞船蓝图**
   - 进入 `Player` 文件夹
   - 右键 → `Blueprint Class`
   - 搜索并选择 `SpaceShip` (你的 C++ 类)
   - 命名为 `BP_SpaceShip`

3. **配置飞船组件**
   
   a) **打开蓝图**
   - 双击 `BP_SpaceShip`
   
   b) **添加可视化网格**
   - 在 `Components` 面板中，点击 `Add Component`
   - 搜索并添加 `Static Mesh`
   - 重命名为 `ShipMesh`
   - 拖拽 `ShipMesh` 到 `CapsuleComponent` 上（作为子组件）
   
   **💡 为什么用 Static Mesh？**
   - Character 类自带一个 Skeletal Mesh Component（用于骨骼动画）
   - 但我们的 2D 俯视角游戏不需要骨骼动画
   - Static Mesh 性能更好，渲染开销更小
   - C++ 代码已经隐藏了默认的 Skeletal Mesh
   - 在蓝图中添加 Static Mesh 用于视觉显示
   
   c) **配置网格**
   - 选中 `ShipMesh`
   - 在 `Details` 面板找到 `Static Mesh` 属性
   - 点击下拉菜单，选择一个临时网格（例如 `Cone` 或 `Arrow`）
   - **重要**: 旋转网格，使尖端朝向 X 轴正方向
     - 在 `Transform` → `Rotation`
     - 设置 `Yaw (Z)` = `0`
     - 根据选择的网格调整 `Pitch (Y)` 和 `Roll (X)`
   - 设置缩放（Scale）：`X=1.0, Y=1.0, Z=1.0` 或根据需要调整
   
   d) **配置碰撞**
   - 选中 `CapsuleComponent`（根组件）
   - `Capsule Half Height` = `50.0`
   - `Capsule Radius` = `30.0`
   - `Collision Presets` = `Pawn`
   
   e) **配置 GAS 能力**
   - 在 `Details` 面板，找到 `Abilities` 分类
   - 找到 `Fire Ability Class` 属性
   - 点击下拉菜单，选择 `GA_Fire`（稍后我们会创建蓝图版本，现在先用 C++ 类）
   - **注意**: 如果找不到，说明 GAS 插件可能没有正确启用
   
   f) **配置射击参数**
   - 找到 `Combat` 分类
   - `Fire Rate` = `0.2` (每秒 5 发)
   
   g) **配置移动参数**
   - 找到 `Movement` 分类
   - `Rotation Speed` = `10.0`

4. **保存**
   - 点击 `Compile` → `Save`
   - 暂时不要关闭（稍后还会回来）

---

### 1.3 创建敌人蓝图

**步骤**：

1. **创建 Enemy 文件夹**
   - 回到 `Blueprints` 文件夹
   - 创建新文件夹：`Enemy`

2. **创建敌人蓝图**
   - 进入 `Enemy` 文件夹
   - 右键 → `Blueprint Class`
   - 搜索并选择 `EnemyBase` (你的 C++ 类)
   - 命名为 `BP_EnemyBase`

3. **配置敌人组件**
   
   a) **打开蓝图**
   - 双击 `BP_EnemyBase`
   
   b) **添加可视化网格**
   - 添加 `Static Mesh` 组件
   - 重命名为 `EnemyMesh`
   - 设置为 `CapsuleComponent` 的子组件
   
   **💡 说明**: 和玩家一样，使用 Static Mesh 而不是 Skeletal Mesh，性能更好
   
   c) **配置网格**
   - 选中 `EnemyMesh`
   - `Static Mesh` = 选择一个临时网格（例如 `Cube` 或 `Sphere`）
   - 设置材质颜色为红色或其他明显颜色（区分敌我）
     - 在 `Materials` 下，点击材质槽
     - 创建新材质实例或使用引擎自带的彩色材质
   - 缩放：`X=0.5, Y=0.5, Z=0.5`（比玩家小一些）
   
   d) **配置碰撞**
   - 选中 `CapsuleComponent`
   - `Capsule Half Height` = `40.0`
   - `Capsule Radius` = `25.0`
   - `Collision Presets` = `Pawn`
   - **重要**: 确保 `Generate Overlap Events` = `True`
   
   e) **配置战斗参数**
   - 找到 `Combat` 分类
   - `Contact Damage` = `10.0`（接触玩家造成的伤害）

4. **保存**
   - 点击 `Compile` → `Save` → 关闭

---

### 1.4 创建子弹蓝图

**步骤**：

1. **创建 Weapons 文件夹**
   - 回到 `Blueprints` 文件夹
   - 创建新文件夹：`Weapons`

2. **创建子弹蓝图**
   - 进入 `Weapons` 文件夹
   - 右键 → `Blueprint Class`
   - 搜索并选择 `Projectile` (你的 C++ 类)
   - 命名为 `BP_Projectile`

3. **配置子弹组件**
   
   a) **打开蓝图**
   - 双击 `BP_Projectile`
   
   b) **添加可视化网格**
   - 添加 `Static Mesh` 组件
   - 重命名为 `ProjectileMesh`
   - 设置为 `CollisionComponent`（SphereComponent）的子组件
   
   c) **配置网格**
   - 选中 `ProjectileMesh`
   - `Static Mesh` = 选择 `Sphere` 或自定义子弹模型
   - 材质设置为明亮的颜色（黄色/白色）便于观察
   - 缩放：`X=0.2, Y=0.2, Z=0.2`（小子弹）
   
   d) **配置碰撞**
   - 选中 `CollisionComponent` (根组件)
   - `Sphere Radius` = `15.0`
   - `Collision Presets` = `Projectile`
   - 如果没有 Projectile 预设，使用 `OverlapAllDynamic` 或自定义：
     - `Collision Enabled` = `Query Only`
     - `Object Type` = `WorldDynamic`
     - 设置对 `Pawn` 通道 = `Overlap`
   
   e) **配置运动参数**
   - 找到 `Movement` 分类
   - `Speed` = `2000.0`（子弹速度）
   
   f) **配置战斗参数**
   - 找到 `Combat` 分类
   - `Damage` = `10.0`（基础伤害）
   
   g) **配置生命周期**
   - 找到 `Projectile` 分类
   - `Lifetime` = `3.0`（3秒后自动销毁）

4. **保存**
   - 点击 `Compile` → `Save` → 关闭

---

### 1.5 创建射击能力蓝图

**步骤**：

1. **创建 Abilities 文件夹**
   - 回到 `Blueprints` 文件夹
   - 创建新文件夹：`Abilities`

2. **创建能力蓝图**
   - 进入 `Abilities` 文件夹
   - 右键 → `Blueprint Class`
   - 搜索并选择 `GA_Fire` (你的 C++ GameplayAbility 类)
   - 命名为 `BP_GA_Fire`

3. **配置能力**
   
   a) **打开蓝图**
   - 双击 `BP_GA_Fire`
   
   b) **配置投射物类**
   - 在 `Details` 面板，找到 `Projectile` 分类
   - 找到 `Projectile Class` 属性
   - 点击下拉菜单，选择 `BP_Projectile`（我们刚创建的）
   
   c) **配置生成位置**
   - `Spawn Offset` = `100.0`（在飞船前方 100 单位生成子弹）
   
   d) **配置音效（可选）**
   - `Fire Sound` = 选择一个射击音效（如果有）
   - 暂时可以留空

4. **保存**
   - 点击 `Compile` → `Save` → 关闭

5. **更新飞船蓝图**
   - 重新打开 `BP_SpaceShip`
   - 找到 `Fire Ability Class` 属性
   - 改为选择 `BP_GA_Fire`（蓝图版本）
   - 点击 `Compile` → `Save` → 关闭

---

### ✅ 第一阶段完成检查

确认你已创建以下文件：
- [ ] `Content/Blueprints/Core/BP_SpaceRoguelikeGameMode`
- [ ] `Content/Blueprints/Player/BP_SpaceShip`
- [ ] `Content/Blueprints/Enemy/BP_EnemyBase`
- [ ] `Content/Blueprints/Weapons/BP_Projectile`
- [ ] `Content/Blueprints/Abilities/BP_GA_Fire`

---

## 🎮 第二阶段：配置输入系统

### 2.1 创建 Input 文件夹结构

**步骤**：

1. **创建 Input 文件夹**
   - 在 `Content` 根目录
   - 右键 → `New Folder`
   - 命名为 `Input`

2. **进入 Input 文件夹**
   - 双击打开 `Input` 文件夹

---

### 2.2 创建输入动作 (Input Actions)

**步骤**：

1. **创建移动输入动作**
   
   a) **创建资产**
   - 在 `Input` 文件夹内，右键
   - 选择 `Input` → `Input Action`
   - 命名为 `IA_Move`
   
   b) **配置输入动作**
   - 双击打开 `IA_Move`
   - `Value Type` = `Axis 2D (Vector 2D)`
     - 这允许同时输入 X 和 Y 方向
   - 点击 `Save` → 关闭

2. **创建射击输入动作**
   
   a) **创建资产**
   - 在 `Input` 文件夹内，右键
   - 选择 `Input` → `Input Action`
   - 命名为 `IA_Fire`
   
   b) **配置输入动作**
   - 双击打开 `IA_Fire`
   - `Value Type` = `Digital (bool)`
     - 这是简单的按下/松开输入
   - 点击 `Save` → 关闭

3. **创建暂停输入动作**
   
   a) **创建资产**
   - 在 `Input` 文件夹内，右键
   - 选择 `Input` → `Input Action`
   - 命名为 `IA_Pause`
   
   b) **配置输入动作**
   - 双击打开 `IA_Pause`
   - `Value Type` = `Digital (bool)`
   - 点击 `Save` → 关闭

---

### 2.3 创建输入映射上下文 (Input Mapping Context)

**步骤**：

1. **创建 IMC**
   - 在 `Input` 文件夹内，右键
   - 选择 `Input` → `Input Mapping Context`
   - 命名为 `IMC_Gameplay`

2. **配置映射**
   
   a) **打开 IMC**
   - 双击打开 `IMC_Gameplay`
   
   b) **添加移动映射**
   - 点击 `+ Mappings` 按钮
   - 在新出现的行中：
     - `Action` = 选择 `IA_Move`
     - 点击 `+` 添加键位绑定
   
   **绑定 W 键（向前）**:
   - `Key` = `W`
   - 点击该键位右边的 `Modifiers` 小箭头展开
   - 添加修改器：点击 `+ Modifiers`
   - 选择 `Swizzle Input Axis Values`
     - `Order` = `YXZ`（将 Y 轴映射到 X）
   
   **绑定 S 键（向后）**:
   - 点击同一个 `IA_Move` 行的 `+` 再添加一个键位
   - `Key` = `S`
   - 添加修改器：`Swizzle Input Axis Values`
     - `Order` = `YXZ`
   - 再添加修改器：`Negate`（取反，向后移动）
   
   **绑定 A 键（向左）**:
   - 点击 `+` 添加键位
   - `Key` = `A`
   - 添加修改器：`Negate`（取反，向左移动）
   
   **绑定 D 键（向右）**:
   - 点击 `+` 添加键位
   - `Key` = `D`
   - 不需要修改器
   
   c) **添加射击映射**
   - 点击 `+ Mappings` 添加新的映射行
   - `Action` = 选择 `IA_Fire`
   - 点击 `+` 添加键位
   - `Key` = `Left Mouse Button`（鼠标左键）
   
   d) **添加暂停映射**
   - 点击 `+ Mappings`
   - `Action` = 选择 `IA_Pause`
   - 点击 `+` 添加键位
   - `Key` = `Escape` 或 `P`

3. **保存**
   - 点击 `Save` → 关闭

---

### 2.4 配置 PlayerController

**步骤**：

1. **重新打开 GameMode**
   - 打开 `BP_SpaceRoguelikeGameMode`

2. **创建 PlayerController 蓝图**
   - 在 `Blueprints/Core` 文件夹
   - 右键 → `Blueprint Class`
   - 搜索并选择 `SpaceRoguelikePlayerController`
   - 命名为 `BP_SpaceRoguelikePlayerController`

3. **配置 Input 设置**
   
   a) **打开 PlayerController**
   - 双击 `BP_SpaceRoguelikePlayerController`
   
   b) **设置 Input Mapping Context**
   - 在 `Details` 面板，找到 `Input` 分类
   - `Default Mapping Context` = 选择 `IMC_Gameplay`
   
   c) **设置 Input Actions**
   - `Move Action` = 选择 `IA_Move`
   - `Fire Action` = 选择 `IA_Fire`
   - `Pause Action` = 选择 `IA_Pause`
   
   d) **保存**
   - 点击 `Compile` → `Save` → 关闭

4. **更新 GameMode**
   - 打开 `BP_SpaceRoguelikeGameMode`
   - 找到 `Classes` 分类
   - `Player Controller Class` = 选择 `BP_SpaceRoguelikePlayerController`
   - `Default Pawn Class` = 选择 `BP_SpaceShip`
   - 点击 `Compile` → `Save` → 关闭

---

### ✅ 第二阶段完成检查

确认你已创建并配置：
- [ ] `Content/Input/IA_Move`（2D Vector）
- [ ] `Content/Input/IA_Fire`（Digital）
- [ ] `Content/Input/IA_Pause`（Digital）
- [ ] `Content/Input/IMC_Gameplay`（已绑定 WASD 和鼠标）
- [ ] `Content/Blueprints/Core/BP_SpaceRoguelikePlayerController`（已配置 Input）
- [ ] `BP_SpaceRoguelikeGameMode` 已设置 PlayerController 和 DefaultPawn

---

## 🎨 第三阶段：创建 UI

### 3.1 创建 UI 文件夹

**步骤**：

1. **创建 Widgets 文件夹**
   - 在 `Content` 根目录
   - 右键 → `New Folder`
   - 命名为 `Widgets`

---

### 3.2 创建游戏 HUD

**步骤**：

1. **创建 Widget Blueprint**
   - 在 `Widgets` 文件夹内
   - 右键 → `User Interface` → `Widget Blueprint`
   - 命名为 `WBP_GameHUD`

2. **设计 UI 布局**
   
   a) **打开 Widget**
   - 双击 `WBP_GameHUD`
   
   b) **添加 Canvas Panel**（如果没有）
   - 在 `Palette` 面板，搜索 `Canvas Panel`
   - 拖拽到 `Hierarchy` 面板的根位置
   
   c) **添加生命值条**
   
   **步骤**:
   - 在 `Palette` 搜索 `Progress Bar`
   - 拖拽到 `Canvas Panel` 下
   - 重命名为 `HealthBar`
   
   **配置位置**:
   - 选中 `HealthBar`
   - 在 `Details` 面板，找到 `Slot (Canvas Panel Slot)` 分类
   - `Anchors` = 点击预设，选择左上角锚点
   - `Position X` = `20`
   - `Position Y` = `20`
   - `Size X` = `300`
   - `Size Y` = `30`
   
   **配置样式**:
   - 找到 `Appearance` 分类
   - `Percent` = `1.0`（临时设为满血）
   - `Fill Color and Opacity` = 绿色（R=0, G=1, B=0, A=1）
   - `Background Color` = 深灰色（R=0.1, G=0.1, B=0.1, A=1）
   
   d) **添加生命值文本**
   
   **步骤**:
   - 在 `Palette` 搜索 `Text`
   - 拖拽到 `HealthBar` 上（作为子组件）
   - 重命名为 `HealthText`
   
   **配置位置**:
   - `Anchors` = 填充整个父组件（Full）
   - `Offsets` = 全部设为 `0`
   
   **配置样式**:
   - `Text` = `HP: 100 / 100`
   - `Font Size` = `16`
   - `Justification` = `Align Text Center`（居中）
   - `Color and Opacity` = 白色
   
   e) **添加击杀计数**
   
   **步骤**:
   - 添加 `Text` 组件到 `Canvas Panel`
   - 重命名为 `KillCountText`
   
   **配置位置**:
   - `Anchors` = 右上角
   - `Position X` = `-200`
   - `Position Y` = `20`
   - `Size X` = `180`
   - `Size Y` = `30`
   
   **配置样式**:
   - `Text` = `Kills: 0`
   - `Font Size` = `20`
   - `Justification` = `Align Text Right`
   - `Color and Opacity` = 白色
   
   f) **添加游戏时间**
   
   **步骤**:
   - 添加 `Text` 组件到 `Canvas Panel`
   - 重命名为 `TimeText`
   
   **配置位置**:
   - `Anchors` = 顶部中央
   - `Alignment` = `X=0.5, Y=0`（居中对齐）
   - `Position X` = `-100`
   - `Position Y` = `20`
   - `Size X` = `200`
   - `Size Y` = `30`
   
   **配置样式**:
   - `Text` = `Time: 00:00`
   - `Font Size` = `24`
   - `Justification` = `Align Text Center`
   - `Color and Opacity` = 白色
   
   g) **添加准星（可选）**
   
   **步骤**:
   - 添加 `Image` 组件到 `Canvas Panel`
   - 重命名为 `Crosshair`
   
   **配置位置**:
   - `Anchors` = 屏幕中心
   - `Alignment` = `X=0.5, Y=0.5`
   - `Position X` = `-10`
   - `Position Y` = `-10`
   - `Size X` = `20`
   - `Size Y` = `20`
   
   **配置样式**:
   - `Brush` → `Tint` = 半透明白色（R=1, G=1, B=1, A=0.5）
   - 如果有准星图片，设置 `Image`；否则用简单的圆形或十字

3. **设置为变量（重要！）**
   
   为了能从代码更新UI，需要将组件设置为变量：
   
   - 选中 `HealthBar`，勾选右上角的 `Is Variable` 复选框
   - 选中 `HealthText`，勾选 `Is Variable`
   - 选中 `KillCountText`，勾选 `Is Variable`
   - 选中 `TimeText`，勾选 `Is Variable`

4. **添加更新逻辑（蓝图脚本）**
   
   a) **切换到 Graph 视图**
   - 点击右上角的 `Graph` 按钮
   
   b) **创建 Tick 事件**
   - 在空白处右键
   - 搜索并添加 `Event Tick`
   
   c) **更新生命值（示例）**
   
   **这部分比较复杂，暂时可以跳过，先测试基本功能**
   
   或者简单实现：
   - 从 `Event Tick` 拉出连线
   - 搜索并添加 `Get Player Pawn`
   - 从返回值拉出，搜索 `Cast to SpaceShip`
   - 如果成功，获取其 AttributeSet
   - 更新 UI 文本
   
   **注意**: 详细的数据绑定较复杂，建议先测试游戏能否运行

5. **保存**
   - 点击 `Compile` → `Save` → 关闭

---

### 3.3 创建 Game Over UI

**步骤**：

1. **创建 Widget Blueprint**
   - 在 `Widgets` 文件夹内
   - 右键 → `User Interface` → `Widget Blueprint`
   - 命名为 `WBP_GameOver`

2. **设计布局**
   
   a) **打开 Widget**
   - 双击 `WBP_GameOver`
   
   b) **添加背景遮罩**
   - 添加 `Image` 到 `Canvas Panel`
   - 重命名为 `Background`
   - `Anchors` = 填充屏幕
   - `Offsets` = 全部 `0`
   - `Color and Opacity` = 半透明黑色（R=0, G=0, B=0, A=0.7）
   
   c) **添加 Game Over 文本**
   - 添加 `Text` 到 `Canvas Panel`
   - 重命名为 `GameOverText`
   - `Anchors` = 顶部中央
   - `Alignment` = `X=0.5, Y=0`
   - `Position Y` = `200`
   - `Text` = `GAME OVER`
   - `Font Size` = `72`
   - `Justification` = 居中
   - `Color` = 红色
   
   d) **添加统计信息**
   - 添加 `Text`，重命名为 `StatsText`
   - `Text` = `Survived: 00:00\nKills: 0`
   - `Font Size` = `32`
   - 居中显示
   
   e) **添加重启按钮**
   - 在 `Palette` 搜索 `Button`
   - 添加到 `Canvas Panel`
   - 重命名为 `RestartButton`
   - 配置位置在屏幕中下方
   
   **按钮文本**:
   - 展开 `RestartButton` 层级
   - 选中里面的 `Text` 组件
   - 修改文本为 `RESTART`
   - `Font Size` = `36`
   
   f) **添加按钮点击事件**
   - 切换到 `Graph` 视图
   - 在 `My Blueprint` 面板，找到 `RestartButton`
   - 右键 → `Add Event` → `On Clicked`
   - 在事件节点后：
     - 添加 `Get Game Mode`
     - `Cast to SpaceRoguelikeGameMode`
     - 调用 `RestartGame` 函数

3. **保存**
   - 点击 `Compile` → `Save` → 关闭

---

### 3.4 配置 UI 显示

**步骤**：

1. **更新 PlayerController**
   - 打开 `BP_SpaceRoguelikePlayerController`
   - 在 `Details` 面板，找到 `UI` 分类
   - `HUD Widget Class` = 选择 `WBP_GameHUD`
   - `Game Over Widget Class` = 选择 `WBP_GameOver`
   - 点击 `Compile` → `Save` → 关闭

---

### ✅ 第三阶段完成检查

确认你已创建：
- [ ] `Content/Widgets/WBP_GameHUD`（包含生命值、击杀数、时间）
- [ ] `Content/Widgets/WBP_GameOver`（包含重启按钮）
- [ ] `BP_SpaceRoguelikePlayerController` 已设置 Widget 类

---

## 🗺️ 第四阶段：创建游戏关卡

### 4.1 创建新关卡

**步骤**：

1. **创建关卡**
   - 点击菜单 `File` → `New Level`
   - 选择 `Empty Level`（空白关卡）
   - 点击 `Create`

2. **保存关卡**
   - 点击 `File` → `Save Current Level As...`
   - 保存位置：`Content/Maps/`（如果没有 Maps 文件夹，创建一个）
   - 命名为 `L_MainGame`

---

### 4.2 配置关卡设置

**步骤**：

1. **打开 World Settings**
   - 点击菜单 `Window` → `World Settings`
   - 如果看不到面板，拖拽调整位置

2. **设置 GameMode**
   - 在 `World Settings` 面板
   - 找到 `Game Mode` 分类
   - `GameMode Override` = 选择 `BP_SpaceRoguelikeGameMode`

3. **配置 World Partition（可选，推荐）**
   
   **World Partition 用于大地图流式加载，MVP 可暂时跳过**
   
   如果要启用：
   - 在 `World Settings` 找到 `World Partition Setup`
   - 点击 `Enable World Partition`
   - 使用默认设置

---

### 4.3 添加基本环境

**步骤**：

1. **添加 Player Start**
   
   a) **放置 Player Start**
   - 在 `Place Actors` 面板（左侧）
   - 搜索 `Player Start`
   - 拖拽到关卡中心
   
   b) **配置位置**
   - 选中 `Player Start`
   - 在 `Details` 面板设置 `Transform`
   - `Location` = `X=0, Y=0, Z=100`
   - `Rotation` = `X=0, Y=0, Z=0`

2. **添加光源**
   
   a) **添加 Directional Light**
   - 在 `Place Actors` 面板
   - 搜索 `Directional Light`
   - 拖拽到关卡
   - `Rotation` 设置为 `Pitch=-45, Yaw=0, Roll=0`
   
   b) **配置光源**
   - `Intensity` = `3.0`
   - `Light Color` = 白色或微蓝色（太空感）

3. **添加天空盒（可选）**
   
   a) **添加 Sky Sphere**
   - 搜索 `Sky Sphere`
   - 拖拽到关卡
   - 保持默认设置
   
   或者
   
   b) **添加 Sky Atmosphere**（UE5 推荐）
   - 搜索 `Sky Atmosphere`
   - 拖拽到关卡
   - 配合 Directional Light 使用

4. **添加地面平面（可选）**
   
   **用于测试碰撞和移动**
   
   - 在 `Place Actors` 面板
   - 找到 `Geometry` 分类
   - 拖拽 `Plane` 到关卡
   - `Scale` = `X=50, Y=50, Z=1`（大地面）
   - `Location` = `Z=-50`（在玩家下方）
   - 设置材质为深色（太空背景）

5. **添加测试障碍物（可选）**
   
   - 添加几个 `Cube` 或 `Sphere`
   - 随机放置在地图中
   - 用于测试碰撞
   - 数量：5-10 个即可

6. **调整相机视角**
   
   **设置顶视角（俯视）**
   
   - 在关卡编辑器中，使用 `鼠标中键` 或 `Alt + 鼠标左键` 旋转视角
   - 调整为俯视视角，方便观察游戏
   - 使用 `鼠标滚轮` 缩放距离

---

### 4.4 配置敌人生成系统

**重要**: 敌人生成系统使用 World Subsystem，需要在关卡中配置

**步骤**：

1. **创建 GameState 蓝图**
   
   a) **创建蓝图**
   - 在 `Blueprints/Core` 文件夹
   - 右键 → `Blueprint Class`
   - 搜索并选择 `SpaceRoguelikeGameState`
   - 命名为 `BP_SpaceRoguelikeGameState`
   
   b) **更新 GameMode**
   - 打开 `BP_SpaceRoguelikeGameMode`
   - `Game State Class` = 选择 `BP_SpaceRoguelikeGameState`
   - 保存

2. **配置 Enemy Spawn Subsystem**
   
   **EnemySpawnSubsystem 是 World Subsystem，自动存在**
   
   但需要在 `BP_SpaceRoguelikeGameMode` 的 `BeginPlay` 中初始化：
   
   a) **打开 GameMode 蓝图**
   - 双击 `BP_SpaceRoguelikeGameMode`
   - 切换到 `Event Graph`
   
   b) **设置敌人类引用**
   
   **方法 1: 通过蓝图设置（推荐）**
   
   - 添加 `Event BeginPlay` 节点
   - 从中拉出，搜索并添加 `Get Game Instance Subsystem`
   - `Class` = `EnemySpawnSubsystem`
   - 从返回值拉出，调用 `Set Enemy Class`
   - 设置为 `BP_EnemyBase`
   
   **方法 2: 在 C++ 中已经硬编码**
   
   如果 C++ 代码中已经设置了 EnemyClass，可以跳过此步骤

3. **保存所有资产**
   - `File` → `Save All`

---

### ✅ 第四阶段完成检查

确认你的关卡包含：
- [ ] `Content/Maps/L_MainGame` 已创建并保存
- [ ] 关卡已设置 GameMode Override 为 `BP_SpaceRoguelikeGameMode`
- [ ] 放置了 `Player Start`
- [ ] 添加了光源（Directional Light）
- [ ] （可选）添加了天空盒
- [ ] （可选）添加了地面和障碍物
- [ ] `BP_SpaceRoguelikeGameMode` 已设置所有必要的 Class 引用

---

## 🎮 第五阶段：项目设置

### 5.1 设置默认关卡

**步骤**：

1. **打开项目设置**
   - 点击菜单 `Edit` → `Project Settings`

2. **设置默认地图**
   - 在左侧列表，找到 `Maps & Modes` 分类
   - `Default Maps`
     - `Editor Startup Map` = `L_MainGame`
     - `Game Default Map` = `L_MainGame`

3. **设置默认 GameMode**
   - 在同一个 `Maps & Modes` 页面
   - `Default GameMode` = `BP_SpaceRoguelikeGameMode`

4. **保存设置**
   - 关闭 `Project Settings` 窗口（自动保存）

---

### 5.2 配置输入设置（可选）

**如果 Enhanced Input 工作不正常，检查以下设置**：

1. **启用 Enhanced Input**
   - 在 `Project Settings`
   - 找到 `Engine` → `Input`
   - `Default Input Component Class` = `Enhanced Input Component`
   - `Default Player Input Class` = `Enhanced Player Input`

---

### 5.3 配置碰撞通道（可选但推荐）

**为了让碰撞系统工作正常**：

1. **打开碰撞设置**
   - 在 `Project Settings`
   - 找到 `Engine` → `Collision`

2. **添加 Projectile 预设（如果没有）**
   - 在 `Preset` 列表中
   - 点击 `+ New Preset`
   - 命名为 `Projectile`
   - 配置：
     - `Collision Enabled` = `Query Only`
     - `Object Type` = `WorldDynamic`
     - 对 `Pawn` 通道 = `Overlap`
     - 对 `WorldStatic` 通道 = `Block`（撞墙消失）

3. **保存设置**

---

### ✅ 第五阶段完成检查

确认项目设置：
- [ ] 默认地图设置为 `L_MainGame`
- [ ] 默认 GameMode 设置为 `BP_SpaceRoguelikeGameMode`
- [ ] Enhanced Input 已正确配置
- [ ] （可选）Projectile 碰撞预设已创建

---

## 🚀 第六阶段：测试游戏

### 6.1 第一次运行

**步骤**：

1. **保存所有内容**
   - 点击 `File` → `Save All`
   - 确认所有资产都已保存

2. **启动游戏**
   - 点击工具栏上的绿色 `Play` 按钮（▶）
   - 或按快捷键 `Alt + P`

3. **选择游戏模式**
   - 选择 `Selected Viewport`（在编辑器视口中运行）
   - 或 `New Editor Window`（独立窗口）

---

### 6.2 基础功能测试清单

**按以下顺序测试每个功能**：

#### ✅ 测试 1: 玩家生成
- [ ] 游戏启动后，玩家飞船出现在场景中
- [ ] 飞船位置在 Player Start 附近
- [ ] 飞船可见（有网格模型）

**如果失败**：
- 检查 Player Start 是否放置在关卡中
- 检查 GameMode 的 Default Pawn Class 是否设置为 `BP_SpaceShip`
- 检查飞船蓝图的 Static Mesh 是否设置

---

#### ✅ 测试 2: 移动控制
- [ ] 按 `W` 键，飞船向上移动
- [ ] 按 `S` 键，飞船向下移动
- [ ] 按 `A` 键，飞船向左移动
- [ ] 按 `D` 键，飞船向右移动
- [ ] 移动流畅，没有卡顿

**如果失败**：
- 检查 PlayerController 的 Default Mapping Context 是否设置
- 检查 IMC_Gameplay 的按键绑定是否正确
- 在 `Output Log` 查看是否有输入相关错误
- 检查飞船的 Character Movement 组件是否配置正确

---

#### ✅ 测试 3: 鼠标朝向
- [ ] 移动鼠标，飞船旋转朝向鼠标位置
- [ ] 旋转平滑，不突兀

**如果失败**：
- 检查 SpaceShip 的 Rotation Speed 参数
- 确认鼠标在视口内（鼠标离开窗口会失效）

---

#### ✅ 测试 4: 射击功能
- [ ] 按住鼠标左键，飞船发射子弹
- [ ] 子弹从飞船前方生成
- [ ] 子弹沿飞船朝向飞行
- [ ] 子弹速度合理
- [ ] 子弹 3 秒后自动消失

**如果失败**：
- 检查 `BP_GA_Fire` 的 Projectile Class 是否设置为 `BP_Projectile`
- 检查 `BP_SpaceShip` 的 Fire Ability Class 是否设置为 `BP_GA_Fire`
- 检查 `BP_Projectile` 的 Projectile Movement Component
- 在 `Output Log` 查看是否有 GAS 相关错误

---

#### ✅ 测试 5: 敌人生成
- [ ] 游戏开始后，敌人自动生成
- [ ] 敌人在玩家视野外生成（屏幕边缘）
- [ ] 生成频率逐渐加快
- [ ] 最多同时有 50 个敌人（默认限制）

**如果失败**：
- 检查 EnemySpawnSubsystem 是否正确初始化
- 检查 `BP_EnemyBase` 是否正确配置
- 在 GameMode 的 BeginPlay 中检查 Enemy Class 设置
- 在 `Output Log` 搜索 "Enemy" 相关日志

---

#### ✅ 测试 6: 敌人 AI
- [ ] 敌人生成后，朝玩家移动
- [ ] 敌人追踪玩家
- [ ] 敌人移动速度合理（比玩家慢）

**如果失败**：
- 检查 `BP_EnemyBase` 的 AI Controller Class 是否自动设置
- 确认 EnemyAIController 代码正确编译
- 检查敌人的 Auto Possess AI 设置

---

#### ✅ 测试 7: 战斗系统
- [ ] 子弹击中敌人，敌人受到伤害
- [ ] 敌人生命值降为 0 后死亡消失
- [ ] 敌人接触玩家，玩家受到伤害
- [ ] 玩家生命值降为 0 后死亡

**如果失败**：
- 检查子弹和敌人的碰撞设置
- 确认 GAS 的 AttributeSet 正确配置
- 检查伤害应用逻辑（在 Projectile.cpp 和 EnemyBase.cpp 中）
- 使用 `Show Collision` 命令查看碰撞体（按 `~` 键打开控制台）

---

#### ✅ 测试 8: UI 显示
- [ ] HUD 显示在屏幕上
- [ ] 生命值条显示（即使数值不更新）
- [ ] 击杀计数显示
- [ ] 游戏时间显示

**如果失败**：
- 检查 PlayerController 的 HUD Widget Class 设置
- 确认 WBP_GameHUD 正确创建
- 在 PlayerController 的 BeginPlay 中检查 ShowHUD 调用

---

#### ✅ 测试 9: 游戏结束
- [ ] 玩家死亡后，显示 Game Over 界面
- [ ] Game Over 界面显示统计信息
- [ ] 点击 Restart 按钮，游戏重新开始

**如果失败**：
- 检查 PlayerController 的 ShowGameOverUI 函数
- 检查 GameMode 的 OnPlayerDeath 调用
- 检查 WBP_GameOver 的重启按钮事件

---

### 6.3 性能测试

**游戏运行时，检查性能指标**：

1. **显示帧率**
   - 按 `~` 键打开控制台
   - 输入命令：`stat fps`
   - 目标：保持在 60 FPS 以上

2. **显示游戏统计**
   - 控制台输入：`stat game`
   - 查看 Actor 数量、Tick 时间等

3. **显示碰撞**
   - 控制台输入：`show collision`
   - 查看所有碰撞体的轮廓

---

### ✅ 第六阶段完成检查

确认所有测试通过：
- [ ] 玩家能正常生成
- [ ] WASD 移动工作
- [ ] 鼠标朝向工作
- [ ] 射击功能工作
- [ ] 敌人自动生成
- [ ] 敌人追踪玩家
- [ ] 战斗系统工作（伤害和死亡）
- [ ] UI 显示
- [ ] 游戏结束和重启

---

## 🐛 故障排除

### 常见问题和解决方案

#### 问题 1: 游戏启动后黑屏，什么都没有

**可能原因**：
1. 关卡中没有光源
2. Player Start 放置在错误位置
3. 相机配置错误

**解决方案**：
1. 添加 Directional Light 到关卡
2. 确认 Player Start 位置在 `Z=100` 或更高
3. 检查 SpaceShip 的 Camera Boom 和 TopDownCamera 组件

---

#### 问题 2: 玩家不能移动

**可能原因**：
1. Input Mapping Context 未设置
2. 按键绑定错误
3. Character Movement 组件未配置

**解决方案**：
1. 检查 PlayerController 的 Default Mapping Context
2. 打开 IMC_Gameplay，验证 WASD 绑定
3. 检查 SpaceShip 的 Character Movement 组件的 Max Walk Speed

---

#### 问题 3: 子弹不发射

**可能原因**：
1. Fire Ability Class 未设置
2. Projectile Class 未设置
3. GAS 未正确初始化

**解决方案**：
1. 打开 BP_SpaceShip，检查 Fire Ability Class = BP_GA_Fire
2. 打开 BP_GA_Fire，检查 Projectile Class = BP_Projectile
3. 在 Output Log 搜索 "Ability" 相关错误
4. 确认 .uproject 文件中已添加 GameplayAbilities 插件

---

#### 问题 4: 敌人不生成

**可能原因**：
1. Enemy Class 未设置
2. EnemySpawnSubsystem 未初始化
3. Enemy Blueprint 配置错误

**解决方案**：
1. 在 GameMode 的 BeginPlay 中设置 Enemy Class
2. 检查 Output Log 中的 "EnemySpawnSubsystem" 日志
3. 手动在关卡中放置一个 BP_EnemyBase，看能否显示

---

#### 问题 5: 碰撞检测不工作

**可能原因**：
1. 碰撞预设配置错误
2. Collision Component 未启用
3. Overlap Events 未开启

**解决方案**：
1. 检查 Projectile 和 Enemy 的 Collision Presets
2. 确认 Generate Overlap Events = True
3. 使用 `show collision` 命令查看碰撞体
4. 在项目设置中配置 Projectile 碰撞通道

---

#### 问题 6: UI 不显示

**可能原因**：
1. Widget Class 未设置
2. Widget 未添加到视口
3. Widget 的 Visibility 设置为 Hidden

**解决方案**：
1. 检查 PlayerController 的 HUD Widget Class
2. 在 PlayerController 的 BeginPlay 中检查 ShowHUD 调用
3. 打开 WBP_GameHUD，检查根组件的 Visibility

---

#### 问题 7: 游戏卡顿或崩溃

**可能原因**：
1. 敌人生成过多
2. 内存泄漏
3. 无限循环

**解决方案**：
1. 检查 EnemySpawnSubsystem 的 Max Active Enemies 限制
2. 确认远距离敌人被正确清理
3. 在 Output Log 查看错误信息
4. 使用 `stat game` 命令查看性能统计

---

## 📊 优化和调整建议

### 平衡性调整

完成基础配置后，可以调整以下参数来平衡游戏：

**玩家参数**（在 BP_SpaceShip 中）:
- `Fire Rate` - 射击速度（默认 0.2 秒）
- `Rotation Speed` - 旋转速度（默认 10.0）
- `Max Walk Speed` - 移动速度（默认 600.0）

**敌人参数**（在 BP_EnemyBase 中）:
- `Contact Damage` - 接触伤害（默认 10.0）
- `Max Walk Speed` - 移动速度（默认 400.0）

**子弹参数**（在 BP_Projectile 中）:
- `Speed` - 子弹速度（默认 2000.0）
- `Damage` - 伤害（默认 10.0）
- `Lifetime` - 生命周期（默认 3.0 秒）

**生成参数**（在 EnemySpawnSubsystem 中，可能需要 C++ 调整）:
- `Initial Spawn Interval` - 初始生成间隔（默认 2.0 秒）
- `Min Spawn Interval` - 最小生成间隔（默认 0.5 秒）
- `Max Active Enemies` - 最大敌人数量（默认 50）
- `Difficulty Adjust Interval` - 难度调整间隔（默认 30 秒）

---

## ✅ 完整配置清单

### 最终检查列表

确认以下所有项目都已完成：

**蓝图类（5个）**:
- [ ] `BP_SpaceRoguelikeGameMode`
- [ ] `BP_SpaceRoguelikeGameState`
- [ ] `BP_SpaceRoguelikePlayerController`
- [ ] `BP_SpaceShip`
- [ ] `BP_EnemyBase`
- [ ] `BP_Projectile`
- [ ] `BP_GA_Fire`

**输入资产（4个）**:
- [ ] `IA_Move`
- [ ] `IA_Fire`
- [ ] `IA_Pause`
- [ ] `IMC_Gameplay`

**UI 资产（2个）**:
- [ ] `WBP_GameHUD`
- [ ] `WBP_GameOver`

**关卡资产（1个）**:
- [ ] `L_MainGame`

**配置设置**:
- [ ] 项目设置中的默认地图
- [ ] 项目设置中的默认 GameMode
- [ ] Enhanced Input 配置
- [ ] 碰撞通道配置

**功能测试**:
- [ ] 所有 9 项基础功能测试通过
- [ ] 性能达标（60+ FPS）

---

## 🎉 恭喜！

如果你完成了以上所有步骤，你应该有一个可以运行的太空 Roguelike 游戏原型了！

### 下一步建议

1. **美化视觉效果**
   - 替换临时网格为真实 3D 模型
   - 添加粒子特效（射击、爆炸）
   - 添加音效和背景音乐

2. **增加游戏内容**
   - 添加更多敌人类型
   - 添加 BOSS 战
   - 添加升级系统

3. **改进 UI**
   - 完善数据绑定（实时更新生命值等）
   - 添加暂停菜单
   - 添加设置界面

4. **优化性能**
   - 实现对象池
   - 优化碰撞检测
   - 添加 LOD 系统

5. **扩展玩法**
   - 添加多种武器
   - 添加技能系统
   - 添加装备系统

---

## 📚 参考资源

### 相关文档
- `SpaceRoguelike_MVP_SystemDesign.md` - 详细的系统设计
- `README_MVP.md` - 项目概述和功能清单
- `MVP_Code_Progress.md` - 代码实现进度

### UE5 官方文档
- [Gameplay Ability System](https://docs.unrealengine.com/5.7/en-US/gameplay-ability-system-for-unreal-engine/)
- [Enhanced Input](https://docs.unrealengine.com/5.7/en-US/enhanced-input-in-unreal-engine/)
- [UMG UI Designer](https://docs.unrealengine.com/5.7/en-US/umg-ui-designer-for-unreal-engine/)

---

## 💬 需要帮助？

如果遇到问题：
1. 检查 `Output Log`（Window → Developer Tools → Output Log）
2. 使用控制台命令调试（按 `~` 键）
3. 参考故障排除章节
4. 查看相关的设计文档

祝你游戏开发顺利！🚀
